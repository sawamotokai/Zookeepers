<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zookeeper</title>
  <link rel="stylesheet" href="/styles.css">
</head>

  <body>
    <div class="page">
      <div class="header">
          <h1 class="title">Zookeeper</h1>
          </p>
      </div>
      
      <h2>Staffs in our zoo</h2>
      <ul>
        <% for (var i =0; i < staffs.length;  i++ ) { %>
          <li/>  <%= staffs[i].Name %> <%= staffs[i].ID %>
          <% } %>
      </ul>
      <h2>New Staff</h2>
      <form action="/staff/new" method="POST" class="form">
        <label for="staffName">Name</label>
        <input id="staffName" type="text" name="name" class="input" placeholder="Bob" required>
        <button type="submit" class="btn">Create!</button>
      </form>
      <div>
        <h4>Clean a Cage</h4>
        <button onClick='cleanCage()'></button>
      </div>
      <button onclick="func1()">Do something!</button>
    </div>
  </body>
  <script>
    function func1(){
      console.log("I am in the click handler 1!"); 
    }
    const cleanCage = (staffId) => {
      // clean cage
    }
    function func2(){
      console.log("I am in the click handler 2!"); 
    }
  </script>

</html>
